/**
 * Enum representing request type
 */
export declare enum ERegistryManagerRequestType {
    Add = 0,
    Change = 1,
    Remove = 2
}
/**
 * Enum representing request status
 */
export declare enum ERegistryManagerRequestStatus {
    Pending = 0,
    Approved = 1,
    Rejected = 2
}
